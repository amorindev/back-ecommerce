package roleservice


/*

func (s *InitService) InitAdmin(ctx context.Context, adminEmail string, adminPass string) error {
	// Buscar roles necesarios
	roles, err := s.RoleRepository.FindByNames(ctx, []string{"admin"})
	if err != nil {
		return err
	}

	if len(roles) == 0 {
		return errors.New("no se encontraron roles necesarios")
	}

	// Crear el usuario administrador con los roles
	// ...
	return nil
}
*/