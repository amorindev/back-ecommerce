package claim

// estamos separando teniendo en cuenta que cada token debe tener unicamente lo necesario
// * de momento lo pondré en general

// ForgotPasswordToken (válido por 1 hora, solo para resetear la contraseña)
/* type ForgotPasswordTokenClaim struct {
	UserID  string `json:"user_id"`
	Email   string `json:"email"`
	TokenType string `json:"token_type"`
	jwt.RegisteredClaims
}
*/
